#include <packer/scheduling.hh>

bool packer::schedule_packages(std::vector<Package*>& packages, const std::unordered_map<std::string, Packerfile>& packerfile_index) {
  bool ok = true;
  packages.clear();

  return ok;
}
